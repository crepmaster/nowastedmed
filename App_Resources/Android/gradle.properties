# 16KB Page Size Compliance Configuration
# This file ensures compatibility with Android 14+ devices using 16KB memory pages

# Force Google Play Services version for geolocation (overrides plugin default)
googlePlayServicesVersion=21.3.0

# Firebase BOM version (consistent across all modules)
firebaseBomVersion=33.13.0
